#version 300 es

uniform mat4 Matrix;

in vec2 inPosition;
in vec2 inTexCoord;
in vec4 inColor;

out vec2 vTexCoord;
out vec4 vColor;

void main()
{
    vTexCoord = inTexCoord;
    vColor = inColor;
    gl_Position = Matrix * vec4(inPosition, 0.0, 1.0);
}
