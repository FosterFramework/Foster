This cmake file downloads and compiles C99 Platform libraries required to run Foster. This is currently only SDL3.

The repo already contains prebuilt SDL3 binaries for x64 Windows/Mac/Linux, but you can build your own (or use your own SDL3 instead).
